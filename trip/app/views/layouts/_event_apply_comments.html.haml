.eventshow__modal-comments
  = link_to image_tag(user.user.avatar, class: "eventshow__modal-comments-avatar"), user_path(user.user_id)
  = link_to "#{user.user.fullname}", class: ".eventshow__modal-comments-name"
  .eventshow__modal-comments-text
    = user.comment
